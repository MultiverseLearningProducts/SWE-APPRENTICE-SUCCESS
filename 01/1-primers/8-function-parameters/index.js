// declare a function called `subtract` that takes two numbers and returns the difference between them
// example: subtract(5, 2) should return 3


// declare a function called `sum` that takes two numbers and returns the sum of them
// example: sum(1, 2) should return 3

// declare a function called `convert` that takes an integer `minutes` and converts it to seconds.
// examples: 
// convert(5) -> 300
// convert(3) -> 180
// convert(2) -> 120

// declare a function called `addition` that takes a number as an argument, increments the number by +1 and returns the result.
// examples:
// addition(0) -> 1
// addition(9) -> 10
// addition(-3) -> -2

// declare a function called `concatenate` that takes two strings and returns the concatenation of the two strings.
// examples:
// concatenate("a", "b") -> "ab"
// concatenate("code", "up") -> "codeup"
// concatenate("connect", "four") -> "connectfour"

// declare a function called 'isEqual' that takes two arguments and returns true if the two arguments are equal. Make sure they are strictly equal.
// examples:
// isEqual(1, 1) -> true
// isEqual(1, 2) -> false
// isEqual("1", 1) -> false
// isEqual("1", "1") -> true
// isEqual("billy", "billy") -> true
// isEqual("billy", "bill") -> false

// declare a function called `max` that takes three numbers and returns the largest number.
// examples:
// max(1, 2, 3) -> 3
// max(5, 3, 1) -> 5
// max(5, 5, 5) -> 5
// max(5, 5, 1) -> 5


// declare a function called `isEmpty` that takes a string and returns true if the string is empty. Otherwise, returns false.
// examples:
// isEmpty("") -> true
// isEmpty(" ") -> false
// isEmpty("a") -> false




export default {
  subtract,
  sum,
  convert,
  addition,
  concatenate,
  isEqual,
  max,
  isEmpty,
}
